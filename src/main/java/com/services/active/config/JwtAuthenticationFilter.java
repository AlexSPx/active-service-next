package com.services.active.config;

// Placeholder to replace WebFlux ServerSecurityContextRepository-based filter.
// Not needed with Spring MVC + OAuth2 Resource Server JWT configuration.
public class JwtAuthenticationFilter {
    // intentionally empty
}
